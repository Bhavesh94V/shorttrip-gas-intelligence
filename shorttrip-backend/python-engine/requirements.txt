# SHORT TRIP GAS PRICE INTELLIGENCE — Python Dependencies
# Install: pip install -r requirements.txt

# Web Framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP Client (async)
httpx>=0.27.0

# Database — use psycopg2-binary (pre-built wheel, no pg_config needed)
psycopg2-binary>=2.9.6

# Gas Price Data (FREE — No API key)
py-gasbuddy>=0.6.0

# Web Scraping (shorttrip.com)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Data & Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Email (built-in smtplib used — no extra package needed)
# WhatsApp & Twilio (install only when keys are ready)
# twilio>=8.0.0         # Uncomment when Twilio configured
